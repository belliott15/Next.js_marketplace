'use client'

const CheckoutButton = () => {
  return (
    <button className='btn btn-primary max-w-[100px]' onClick={() => alert('You checked out yay!')}>Checkout</button>
  )
}

export default CheckoutButton